<div class="row g-3">
  <div class="col-12" style="z-index: 10; position: relative;">
    <div class="card shadow-sm app-header-card">
      <div class="card-body d-flex flex-column flex-sm-row align-items-sm-center justify-content-between gap-3">
        <div>
          <h1 class="h4 mb-1 text-white"><%= @company.name %></h1>
          <p class="text-secondary mb-0">Müşteri Gözlem</p>
        </div>

        <div class="d-flex flex-wrap align-items-center gap-2 app-header-actions">
          <%= edit_record_button(edit_company_path(@company), class_name: "app-header-icon-btn") %>
          <%= list_nav_button(companies_path, class_name: "app-header-icon-btn") %>
          <%= home_nav_button(home_path, class_name: "app-header-icon-btn", icon: "home") %>
        </div>
      </div>
    </div>
  </div>

  <!-- LEVEL 1: INFORMATION CARD -->
  <div class="col-12">
    <div class="card shadow-sm border-0 overflow-hidden">
      <div class="card-header bg-white py-3 border-bottom-0">
        <h5 class="card-title mb-0 d-flex align-items-center gap-2">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
          Genel Bilgiler
        </h5>
      </div>
      <div class="card-body p-0">
        <div class="row g-0 border-top">
          <div class="col-md-3 border-end p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Yetkili</label>
            <div class="fw-semibold"><%= @company.authorized_person.presence || "-" %></div>
          </div>
          <div class="col-md-3 border-end p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Telefon</label>
            <div><%= @company.phone.presence || "-" %></div>
          </div>
          <div class="col-md-3 border-end p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">E-Posta</label>
            <div class="text-break"><%= @company.email.presence || "-" %></div>
          </div>
          <div class="col-md-3 p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Vergi No / Dairesi</label>
            <div><%= @company.tax_number %> / <%= @company.tax_office.presence || "-" %></div>
          </div>
        </div>
        <div class="row g-0 border-top">
          <div class="col-md-3 border-end p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Şehir / Ülke</label>
            <div><%= [@company.city, @company.country].reject(&:blank?).join(" / ").presence || "-" %></div>
          </div>
          <div class="col-md-3 border-end p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Durum</label>
            <div>
              <span class="badge <%= @company.active ? 'bg-success' : 'bg-warning text-dark' %>">
                <%= @company.active ? "Aktif" : "Pasif" %>
              </span>
            </div>
          </div>
          <div class="col-md-6 p-3">
            <label class="small text-uppercase fw-bold text-muted mb-1">Adres</label>
            <div class="text-wrap"><%= @company.address.presence || "-" %></div>
          </div>
        </div>
        <% if @company.description.present? %>
          <div class="row g-0 border-top">
            <div class="col-12 p-3">
              <label class="small text-uppercase fw-bold text-muted mb-1">Açıklama</label>
              <div class="text-wrap"><%= @company.description %></div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- LEVEL 2: DASHBOARD CARD -->
  <div class="col-12">
    <div class="card shadow-sm border-0 overflow-hidden bg-white">
      <div class="card-header bg-white py-3 border-bottom-0">
        <h5 class="card-title mb-0 d-flex align-items-center gap-2">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-success"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
          Ticari Performans Gözlemi
        </h5>
      </div>
      <div class="card-body p-4 pt-0">
        <div class="row g-3">
          <!-- Teklif Özeti -->
          <div class="col-md-6 col-lg-3">
            <div class="p-3 bg-light rounded border shadow-sm h-100">
              <div class="small fw-bold text-secondary text-uppercase mb-2" style="font-size: 0.7rem; letter-spacing: 0.05em;">TOPLAM TEKLİF</div>
              <div class="h3 mb-0 fw-bold"><%= @company.total_offers_count %></div>
              <div class="small text-muted mt-2">Toplam Verilen</div>
            </div>
          </div>
          <div class="col-md-6 col-lg-3">
            <div class="p-3 bg-light rounded border shadow-sm h-100">
              <div class="small fw-bold text-secondary text-uppercase mb-2" style="font-size: 0.7rem; letter-spacing: 0.05em;">KABUL EDİLEN</div>
              <div class="h3 mb-0 text-success fw-bold"><%= @company.approved_offers_count %></div>
              <div class="small text-muted mt-2">Onaylanan Teklifler</div>
            </div>
          </div>
          <div class="col-md-6 col-lg-3">
            <div class="p-3 bg-light rounded border shadow-sm h-100">
              <div class="small fw-bold text-secondary text-uppercase mb-2" style="font-size: 0.7rem; letter-spacing: 0.05em;">TEKLİF HACMİ</div>
              <div class="h3 mb-0 fw-bold"><%= number_to_currency(@company.total_offer_amount, unit: "₺", precision: 0) %></div>
              <div class="small text-muted mt-2">Brüt Toplam</div>
            </div>
          </div>
          <div class="col-md-6 col-lg-3">
            <div class="p-3 bg-light rounded border shadow-sm h-100">
              <div class="small fw-bold text-secondary text-uppercase mb-2" style="font-size: 0.7rem; letter-spacing: 0.05em;">KAZANILAN TUTAR</div>
              <div class="h3 mb-0 text-success fw-bold"><%= number_to_currency(@company.approved_offer_amount, unit: "₺", precision: 0) %></div>
              <div class="small text-muted mt-2">Onaylı Brüt Tot.</div>
            </div>
          </div>
          
          <!-- placeholder Modules (Sales, Orders) -->
          <div class="col-md-6 col-lg-4">
            <div class="p-3 bg-white rounded border border-dashed text-center opacity-75">
              <div class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">Satış / Ciro</div>
              <div class="text-muted small italic" style="font-size: 0.75rem;">Satış Modülü Bekleniyor</div>
            </div>
          </div>
          <div class="col-md-6 col-lg-4">
            <div class="p-3 bg-white rounded border border-dashed text-center opacity-75">
              <div class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">Sipariş Toplamı</div>
              <div class="text-muted small italic" style="font-size: 0.75rem;">Sipariş Modülü Bekleniyor</div>
            </div>
          </div>
          <div class="col-md-6 col-lg-4">
            <div class="p-3 bg-white rounded border border-dashed text-center opacity-75">
              <div class="small fw-bold text-muted text-uppercase mb-1" style="font-size: 0.65rem;">Bekleyen Sipariş</div>
              <div class="text-muted small italic" style="font-size: 0.75rem;">Sipariş Modülü Bekleniyor</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- LEVEL 3: ACTIONS PANEL TRIGGER -->
  <div class="col-12 mt-2 mb-4">
    <div class="d-grid gap-2">
      <button class="btn btn-dark py-3 fw-bold text-uppercase shadow-sm d-flex align-items-center justify-content-center gap-3" 
              type="button" data-bs-toggle="offcanvas" data-bs-target="#companyActionsPanel" aria-controls="companyActionsPanel"
              style="letter-spacing: 0.1em; border-radius: 8px; background: #111;">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
        RT - Müşteri İşlem Paneli
      </button>
    </div>
  </div>
</div>

<!-- OFFCANVAS PANEL (Yan Menü) -->
<div class="offcanvas offcanvas-start border-0 shadow-lg" tabindex="-1" id="companyActionsPanel" aria-labelledby="companyActionsPanelLabel" style="width: 380px;">
  <div class="offcanvas-header bg-dark text-white p-4">
    <div class="d-flex align-items-center gap-3">
      <div class="bg-primary rounded-circle p-2">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
      </div>
      <div>
        <h5 class="offcanvas-title fw-bold" id="companyActionsPanelLabel">Müşteri İşlem Paneli</h5>
        <div class="small text-secondary"><%= @company.name %></div>
      </div>
    </div>
    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
  </div>
  
  <div class="offcanvas-body p-0">
    <div class="list-group list-group-flush">
      <div class="px-4 py-3 bg-light border-bottom">
        <h6 class="text-uppercase fw-bold text-muted small mb-0" style="letter-spacing: 0.1em;">Ticari Hareketler</h6>
      </div>
      
      <a href="#" class="list-group-item list-group-item-action p-4 border-0 rt-arrow-hover d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center gap-3">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
          <span class="fw-semibold">Verilen Teklifler</span>
        </div>
        <span class="badge bg-primary rounded-pill"><%= @company.total_offers_count %></span>
      </a>

      <a href="#" class="list-group-item list-group-item-action p-4 border-0 rt-arrow-hover d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center gap-3">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-success"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6L18 2H6z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>
          <span class="fw-semibold">Verilen Siparişler</span>
        </div>
        <span class="badge bg-light text-dark border">0</span>
      </a>

      <div class="px-4 py-3 bg-light border-bottom border-top mt-2">
        <h6 class="text-uppercase fw-bold text-muted small mb-0" style="letter-spacing: 0.1em;">Muhasebe ve Finans</h6>
      </div>

      <a href="#" class="list-group-item list-group-item-action p-4 border-0 rt-arrow-hover d-flex align-items-center gap-3">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-warning"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
        <span class="fw-semibold">Cari Hesap Dökümü</span>
      </a>

      <a href="#" class="list-group-item list-group-item-action p-4 border-0 rt-arrow-hover d-flex align-items-center gap-3">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-info"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
        <span class="fw-semibold">Ekstre Oluştur (PDF)</span>
      </a>
    </div>
  </div>

  <div class="offcanvas-footer p-4 border-top bg-light">
    <div class="small text-center text-muted italic">Müşteri paneli tüm işlemlerinizi tek noktadan yönetmenizi sağlar.</div>
  </div>
</div>


