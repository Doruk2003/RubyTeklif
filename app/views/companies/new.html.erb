<div class="container-fluid py-2">
  <div class="row g-3">
    <div class="col-12" style="z-index: 10; position: relative;">
      <div class="card shadow-sm">
        <div class="card-body d-flex flex-column flex-sm-row align-items-sm-center justify-content-between gap-3">
          <div>
            <h1 class="h4 mb-1 text-uppercase">YENİ MÜŞTERİ</h1>
            <p class="text-secondary mb-0">Firma kaydı oluştur.</p>
          </div>
          <div class="d-flex flex-wrap align-items-center gap-2 justify-content-end">
            <%= list_nav_button(companies_path, label: "Müşteri Listesi") %>
            <%= home_nav_button(home_path) %>
          </div>
        </div>
      </div>
    </div>

    <div class="col-12">
      <div class="card shadow-sm">
        <div class="card-body">
          <%= render "form", company: @company %>
        </div>
      </div>
    </div>
  </div>
</div>


