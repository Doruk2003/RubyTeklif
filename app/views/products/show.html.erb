<div class="min-h-screen bg-gradient-to-b from-slate-50 to-white px-4 py-3">
  <div class="w-full space-y-4">
    <div class="mt-1.5 rounded-2xl bg-slate-50/90 p-5 shadow-[0_18px_40px_-16px_rgba(15,23,42,0.45)]">
      <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div>
          <h1 class="text-2xl font-semibold tracking-tight text-slate-900">Ürün Detayý</h1>
          <p class="text-sm text-slate-500"><%= @product&.dig("name") || "Ürün bulunamadý" %></p>
        </div>
        <div class="flex w-full flex-col gap-2 sm:w-auto sm:flex-row sm:items-center">
          <%= link_to "Ürün Listesi", products_path, class: "btn-secondary w-full sm:w-auto" %>
          <% if @product.present? %>
            <%= link_to "Düzenle", edit_product_path(@product["id"]), class: "btn-secondary w-full sm:w-auto" %>
            <%= link_to "Sil", product_path(@product["id"]), method: :delete, data: { confirm: "Bu ürünü silmek istediðinize emin misiniz?" }, class: "btn-secondary w-full sm:w-auto" %>
          <% end %>
          <%= link_to "Yeni Ürün", new_product_path, class: "btn-primary w-full sm:w-auto" %>
        </div>
      </div>
    </div>

    <div class="rounded-2xl bg-slate-50/90 p-5 shadow-[0_18px_40px_-16px_rgba(15,23,42,0.45)]">
      <div class="rounded-2xl bg-white p-6 shadow-sm">
        <% if @product.present? %>
          <dl class="grid grid-cols-1 gap-4 md:grid-cols-2">
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Ürün</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= @product["name"].to_s %></dd>
            </div>
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Müþteri</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= @product.dig("companies", "name").to_s.presence || "-" %></dd>
            </div>
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Tip</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= @product["item_type"].to_s %></dd>
            </div>
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Aktif</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= @product["active"] ? "Evet" : "Hayýr" %></dd>
            </div>
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Birim Fiyat</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= number_to_currency(@product["price"], unit: "TL ", format: "%u%n") %></dd>
            </div>
            <div>
              <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">KDV Oraný</dt>
              <dd class="mt-1 text-sm text-slate-900"><%= @product["vat_rate"].to_s %> %</dd>
            </div>
          </dl>
        <% else %>
          <div class="text-sm text-slate-600">Ürün bulunamadý.</div>
        <% end %>
      </div>
    </div>
  </div>
</div>
